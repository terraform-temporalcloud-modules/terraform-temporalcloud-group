# No inputs: this directory is a fixed regression fixture, not a configurable
# example. Every value it passes to the module is hardcoded on purpose so the set
# of inputs under test cannot vary between runs.
